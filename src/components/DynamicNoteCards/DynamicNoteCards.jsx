import { Col, Card } from 'antd'

function DynamicNoteCards() {
  return (
    <Col span={24} style={{ paddingBottom: '3rem' }}>
      <Card style={{ height: '30rem', backgroundColor: '#A3B18A' }}>
        Dinamik Not Kartları
      </Card>
    </Col>)
}

export default DynamicNoteCards;

